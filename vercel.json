{
  "version": 2,
  "builds": [
    {
      "src": "index.html", // O el punto de entrada principal, ajusta si es diferente
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/dist/$1"
    }
  ],
  "buildCommand": "npm install && npm run build"
}